---
layout: layouts/base.njk
---
<h2>{{ title }}</h2>

{{ content | safe }}

<p>
  {% for tag in tags %}
    {% if tag != "posts" %}
      <a class="tag-link" href="/tags/{{ tag | slugify }}/">{{ tag }}</a>
    {% endif %}
  {% endfor %}
  — {{ page.date | dateFormat }}
</p>
